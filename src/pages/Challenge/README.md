# React coding quiz demo

RESTful API

- FootballCompetitions
- `<select value={year} onChange={() => setYear(e.target.value)}></select>`
- MyUserList, RESTful, dropdown, filterUser, `<select>`
- TodoList, RESTful, dropdown, Pagination
- MyPostList, RESTful, dropdown with no duplicate id, Filter
